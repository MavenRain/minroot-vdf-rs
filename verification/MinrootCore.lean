/- Entry point for the minroot-core verification project.

   Imports the Aeneas-extracted field arithmetic (the `reduce_wide`
   shift-and-subtract reduction chain). Proofs live under
   `MinrootCore/Properties/`. -/
import MinrootCore.Code.Funs
import MinrootCore.Properties.ReduceWide
