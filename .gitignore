/target
**/*.rs.bk
.DS_Store

# Lean build cache (Mathlib/Aeneas oleans — multi-GB, regenerated by `lake build`)
verification/.lake/
# Charon extraction artifact (regenerated by `charon cargo --preset=aeneas`)
*.llbc
